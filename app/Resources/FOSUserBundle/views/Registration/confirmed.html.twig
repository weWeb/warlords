{% extends "FOSUserBundle::layout.html.twig" %}

{% block body %}
    <div class="mainitem">
		<div class="header">
            <div class="title"><h1>REGISTRATION CONFIRMED</h1></div>
        </div>
        <div class="content">
            <p>{{ 'registration.confirmed'|trans({'%username%': user.username}, 'FOSUserBundle') }}</p>
            <p><a href="{{ path('WarlordsGameBundle_profile') }}">{{ 'registration.back'|trans({}, 'FOSUserBundle') }}</a></p>
        </div>
		<div class="footer"></div>
	</div>
{% endblock body %}
