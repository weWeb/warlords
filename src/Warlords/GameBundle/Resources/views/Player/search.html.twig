

{% block body %}
	{% if app.user.username is defined %}
		<div class="header">
			<div class="atk-title"><h1>Player Info</h1></div>
		</div>
		<div id="atk-dialog">

			<div class="rival-info">
				<header>
			    		<p>Player: {{ player.user.username }}</p>
				</header>
				<ul id="info">
					<li>Level: {{ player.level }}</li>
					<li>Fame: {{ player.Fame }}</li>
					<li>Gold: {{ player.gold}}</li>

				</ul>

	
			</div>
	    		
	    	</div>
	    	
	    	<div class="search-result">
	    		<div><button type="button" id="btn-ally">Add Ally</button></div>
	    		<div><button type="button" id="btn-attack">Attack</button></div>
	    		
			<div id="target-attack">
	    			{% render 'WarlordsGameBundle:Player:attack' with {'target_id' : player.user.id } %}
		    	</div>
		    	<div id="target-ally">
				{% render 'WarlordsGameBundle:Player:ally' with {'target_id' : player.user.id } %}
			</div>
		</div>
		
		
	    	{% block javascript %}
		<script type="text/javascript"> 
		    $( "[id^=target-]" ).dialog({
		    autoOpen: false,
		    height: 350,
		    minWidth: 450,
		    width: 'auto',
		    modal: true,
		    show: "explode",
		    hide: "explode",
		    open: function() {
		    	$('.ui-widget-overlay').bind('click', function() {
		        		$("[id^=target-]").dialog('close');
		    		})
			}
		});
	 
		$( "#btn-ally" ).click(function() {
		    $( "#target-ally").dialog( "open" );
		    $("[class^=dialog-]").dialog( "close");
		    return false;
		});
		$( "#btn-attack" ).click(function() {
		    $( "#target-attack").dialog( "open" );
		    $("[class^=dialog-]").dialog( "close");
		    return false;
		});

		</script>
		{% endblock %}	        
		
	{% else %}
		<div><p>Please login to access this page</p></div>
	{% endif %}
	
{% endblock %}

{% block javascripts %}
	<script type="text/javascript"> 
	

	$("button").hover(

                function(){$(this).stop(true).animate({
                    backgroundColor: "#ff760f",
  
                }, 'linear' );},

                function(){$(this).stop(true).animate({
                    backgroundColor: "#1f1b18",

                }, 'linear' );}
        );

	$("input[type='submit']").hover(

                function(){$(this).stop(true).animate({
                    backgroundColor: "#ff760f",
  
                }, 'linear' );},

                function(){$(this).stop(true).animate({
                    backgroundColor: "#1f1b18",

                }, 'linear' );}
        );
	</script>  
{% endblock %}




